import { OrdenJuegosPipe } from './orden-juegos.pipe';

describe('OrdenJuegosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenJuegosPipe();
    expect(pipe).toBeTruthy();
  });
});
