import { FiltroPlataformaPipe } from './filtro-plataforma.pipe';

describe('FiltroPlataformaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPlataformaPipe();
    expect(pipe).toBeTruthy();
  });
});
